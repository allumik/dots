#! /bin/sh

# kill lemonbar fist
killall lemonbar

# start lemonbar again
( bash /home/allu/.config/bar/barcode | \
    lemonbar -p -R -g 1920x25 -n "bspwm" -d -u 3 \
    -F#FBF3B5 -B#00272827 -U#E0E0E0 \
    -f "monofur:size=14" -f "Font Awesome:size=15" ) &
